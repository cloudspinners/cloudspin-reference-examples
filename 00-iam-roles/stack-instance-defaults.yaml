stack:
  name: starter_roles
  definition_location: 'https://github.com/cloudspinners/spin-stack-iam-roles/archive/master.zip'

instance:
  group: local

terraform_backend:
  region: eu-west-1
  encrypt: true

parameters:
  managed_stack_name: cloudspin_examples
  region: "eu-west-1"
  environment_name: local
